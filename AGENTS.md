# AGENTS.md

本文件定义 AI Coding Agent 在 Tomoni 项目中必须遵守的基本规则。

## 修改前

1. 先阅读相关代码与对应文档。
2. 先说明问题、涉及模块、预计修改文件和最小方案。
3. 每次只处理一个明确目标。
4. 不修改与任务无关的文件。

## 架构

1. 页面与组件只负责展示和交互。
2. 业务规则放在对应 Feature 的 domain、service 或 use case 中。
3. 第三方服务必须通过 `src/infrastructure` 封装。
4. `FixedSchedule`、`Invitation`、`Meetup` 必须保持独立。
5. 同一业务规则只能有一个权威实现。

## 依赖

1. 默认不新增 production dependency。
2. 先检查原生能力、框架能力和现有依赖。
3. 不引入功能重复的 Library。
4. 不在修复 Bug 时顺便升级无关依赖。
5. 新增依赖前说明用途、替代方案和维护成本。

## 数据与状态

1. 数据库结构变化必须通过 Migration。
2. 状态变化必须遵守状态机。
3. 取消、拒绝、撤回不能通过删除记录表示。
4. 接受邀请前不得公开精确集合地点。
5. 敏感数据不得写入日志。

## 测试

Bug 修复必须添加回归测试。核心状态机必须有单元测试。

完成后实际运行：

```bash
npm run typecheck
npm run lint
npm run build
```

配置测试后还需运行：

```bash
npm test
```

不得声称运行过实际未运行的命令。

## 完成报告

每次任务完成后报告：

- 修改内容
- 修改文件
- 新增依赖
- 数据库变化
- 状态机变化
- 测试结果
- 已知限制
- 人工验证步骤
